<%= form_for(@upload, url: upload_moderation_path(params[:id]), remote: true) do |f| %>
  <%= hidden_field_tag :approved, false %>
	<%= f.button id: "disapprove", class: "icon" do %>
		<i class="fa fa-check fa-lg on"></i>
	<% end %>
<% end %>