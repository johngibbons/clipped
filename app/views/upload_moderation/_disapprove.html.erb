<%= form_for(@upload, url: upload_moderation_path(params[:id]), remote: true) do |f| %>
  <div><%= hidden_field_tag :approved, false %></div>
  <%= f.submit "Disapprove", class: "disapprove-btn" %>
<% end %>