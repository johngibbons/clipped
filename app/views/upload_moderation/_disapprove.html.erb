<%= form_for(upload, url: upload_moderation_path(upload.id), remote: true) do |f| %>
  <%= hidden_field_tag :approved, false %>
  <%= f.button class: "disapprove approval icon on", title: "unapprove" do %>
    <i class="fa fa-check fa-lg on"></i>
  <% end %>
<% end %>
