<%= form_for(current_user.liker_relationships.build, remote: true) do |f| %>
  <%= hidden_field_tag :liked_id, @upload.id %>
	<%= f.button id: "like", class: "icon" do %>
		<i class="fa fa-heart fa-lg off"></i>
	<% end %>
<% end %>