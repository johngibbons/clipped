$('.user-uploads').append('<%= j render partial: "upload", collection: @upload %>');
setTagsEditors();
<% if @uploads.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @uploads %>');
<% else %>
  $('.pagination').remove();
<% end %>
