$('.user-uploads').append('<%= j render @uploads %>');
setTagsEditors();
<% if @uploads.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @uploads %>');
<% else %>
  $('.pagination').remove();
<% end %>
