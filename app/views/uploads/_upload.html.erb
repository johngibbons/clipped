<% present(upload) do |upload| %>
  <%= content_tag :div, class: "upload", data: {id: upload.id} do %>
    <%= link_to upload, class: "upload-thumb", id: "upload-#{upload.id}" do %>
      <%= image_tag upload.src(:thumb), class: "#{upload.processed_class}" %>
    <% end %>
  <% end %>
<% end %>
